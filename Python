Задача: Дан массив чисел [3, 6, 2, 8, 5, 10]. Разделите его на два массива: чётные и нечётные числа.

```python
arr = [3, 6, 2, 8, 5, 10]

even = [x for x in arr if x % 2 == 0]
odd = [x for x in arr if x % 2 != 0]

print("Чётные:", even)
print("Нечётные:", odd)


Задача: Создайте стек, добавьте элементы 'a', 'b', 'c', извлеките верхний элемент и выведите состояние стека.

stack = []
stack.append('a')
stack.append('b')
stack.append('c')

print("Стек до извлечения:", stack)

top = stack.pop()
print("Извлечённый элемент:", top)
print("Стек после извлечения:", stack)


Задача: Создайте связный список с элементами 1, 2, 3, добавьте элемент 4 в конец, выведите список.

class Node:
    def __init__(self, data):
        self.data = data
        self.next = None

class LinkedList:
    def __init__(self):
        self.head = None

    def append(self, data):
        new_node = Node(data)
        if not self.head:
            self.head = new_node
            return
        last = self.head
        while last.next:
            last = last.next
        last.next = new_node

    def print_list(self):
        current = self.head
        while current:
            print(current.data, end=" ")
            current = current.next

ll = LinkedList()
ll.append(1)
ll.append(2)
ll.append(3)
ll.append(4)

ll.print_list()
