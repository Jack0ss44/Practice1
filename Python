Пример 1.
Кучи

import heapq

# создаём пустую кучу
heap = []

# добавляем элементы
heapq.heappush(heap, 10)
heapq.heappush(heap, 3)
heapq.heappush(heap, 7)

# извлечение минимального элемента
min_elem = heapq.heappop(heap)

print("Куча:", heap)
print("Минимальный элемент:", min_elem)


Пример 2.
Хип

import heapq

# список преобразуем в кучу
data = [20, 5, 15, 22, 7]
heapq.heapify(data)   # превращаем список в кучу (min-heap)

# доступ к корню (минимальному элементу)
root = data[0]

# добавление и удаление
heapq.heappush(data, 2)
heapq.heappop(data)

print("Хип:", data)
print("Корень:", root)


Пример 3.
Хеш-таблица

# создаём хеш-таблицу (словарь)
hash_table = {}

# добавление элементов
hash_table["apple"] = 10
hash_table["banana"] = 20

# доступ по ключу
value = hash_table["apple"]

# удаление элемента
del hash_table["banana"]

print("Хеш-таблица:", hash_table)
print("Значение по ключу 'apple':", value)
