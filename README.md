Алгоритмы сортировки

Сортировка выбором (Selection Sort)
Определение: 
Сортировка выбором — это алгоритм, который находит минимальный элемент из неотсортированной части массива и меняет его местами с первым элементом этой части.

Описание: 
На каждом шаге ищется минимальный элемент среди неотсортированных и ставится в начало неотсортированной области.

Пошаговая работа:
1. Начинаем с первого элемента массива.
2. Находим минимальный элемент в неотсортированной части.
3. Меняем найденный минимум с текущим элементом.
4. Увеличиваем отсортированную часть на один элемент.
5. Повторяем, пока весь массив не отсортирован.

Сложность: 
O(n²)

Пример:
```python
def selection_sort(arr):
    for i in range(len(arr)):
        min_idx = i
        for j in range(i + 1, len(arr)):
            if arr[j] < arr[min_idx]:
                min_idx = j
        arr[i], arr[min_idx] = arr[min_idx], arr[i]
    return arr

print(selection_sort([23, 56, 21, 41, 12]))
```
Результат: `[12, 21, 23, 41, 56]`



Сортировка пузырьком (Bubble Sort)
Определение: 
Алгоритм, который многократно проходит по массиву и меняет местами соседние элементы, если они стоят неправильно.

Описание:
После каждого прохода самый большой элемент «всплывает» в конец массива.

Пошаговая работа:
1. Проходим по массиву несколько раз.
2. Сравниваем соседние элементы.
3. Меняем их местами, если порядок неверный.
4. После каждого прохода самый большой элемент оказывается в конце.
5. Повторяем, пока массив не отсортирован.

Сложность: 
Худший и средний случай: O(n²), лучший случай: O(n).

Пример:
```python
def bubble_sort(arr):
    n = len(arr)
    for i in range(n):
        swapped = False
        for j in range(0, n - i - 1):
            if arr[j] > arr[j + 1]:
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
                swapped = True
        if not swapped:
            break
    return arr

print(bubble_sort([64, 34, 25, 12, 22, 11, 90]))
```
Результат: `[11, 12, 22, 25, 34, 64, 90]`



Сортировка вставками (Insertion Sort)
Определение:
Алгоритм вставками формирует отсортированную часть массива, вставляя каждый новый элемент на своё место.

Описание: 
Каждый элемент сравнивается с предыдущими и вставляется в правильную позицию.

Пошаговая работа:
1. Начинаем со второго элемента.
2. Сохраняем его во временной переменной.
3. Сдвигаем все большие элементы вправо.
4. Вставляем элемент в найденную позицию.
5. Повторяем для всех элементов.

Сложность:
Худший и средний случай: O(n²), лучший случай: O(n).

Пример:
```python
def insertion_sort(arr):
    for i in range(1, len(arr)):
        key = arr[i]
        j = i - 1
        while j >= 0 and arr[j] > key:
            arr[j + 1] = arr[j]
            j -= 1
        arr[j + 1] = key
    return arr

print(insertion_sort([15, 8, 42, 4, 23, 16]))
```
Результат: `[4, 8, 15, 16, 23, 42]`



Сортировка слиянием (Merge Sort)
Определение:
Алгоритм «разделяй и властвуй», который делит массив на части, сортирует их и объединяет в один отсортированный.

Описание:
Массив рекурсивно делится пополам, каждая часть сортируется и сливается.

**Пошаговая работа:**
1. Разделяем массив пополам.
2. Рекурсивно сортируем левую и правую половины.
3. Объединяем отсортированные части в один массив.

Сложность: 
O(n log n)

Пример:
```python
def merge_sort(arr):
    if len(arr) > 1:
        mid = len(arr)//2
        left = arr[:mid]
        right = arr[mid:]

        merge_sort(left)
        merge_sort(right)

        i = j = k = 0
        while i < len(left) and j < len(right):
            if left[i] < right[j]:
                arr[k] = left[i]
                i += 1
            else:
                arr[k] = right[j]
                j += 1
            k += 1

        while i < len(left):
            arr[k] = left[i]
            i += 1
            k += 1

        while j < len(right):
            arr[k] = right[j]
            j += 1
            k += 1
    return arr

print(merge_sort([38, 27, 43, 3, 9, 82, 10]))
```
Результат: `[3, 9, 10, 27, 38, 43, 82]`



Сортировка Шелла (Shell Sort)
Определение:
Усовершенствованная версия сортировки вставками, использующая шаг (gap) между сравниваемыми элементами.

Описание:  
Элементы сравниваются и переставляются на определённом расстоянии, шаг постепенно уменьшается.

Пошаговая работа:
1. Выбираем начальный шаг gap = n/2.
2. Сортируем элементы с этим шагом методом вставок.
3. Уменьшаем gap вдвое.
4. Повторяем до gap = 1.

Сложность:
Средняя — O(n log n), худшая — O(n²).

Пример:
```python
def shell_sort(arr):
    n = len(arr)
    gap = n // 2
    while gap > 0:
        for i in range(gap, n):
            temp = arr[i]
            j = i
            while j >= gap and arr[j - gap] > temp:
                arr[j] = arr[j - gap]
                j -= gap
            arr[j] = temp
        gap //= 2
    return arr

print(shell_sort([23, 12, 1, 8, 34, 54, 2, 3]))
```
Результат: `[1, 2, 3, 8, 12, 23, 34, 54]`



 Быстрая сортировка (Quick Sort)
Определение:
Алгоритм "разделяй и властвуй", который выбирает опорный элемент и делит массив на две части: меньшие и большие опорного.

Описание:  
Массив делится по pivot и сортируется рекурсивно.

Пошаговая работа:
1. Выбираем опорный элемент.
2. Разделяем массив на меньшие и большие.
3. Рекурсивно сортируем обе части.
4. Объединяем их.

Сложность:
Средний случай: O(n log n), худший: O(n²).

Пример:
```python
def quick_sort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr)//2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quick_sort(left) + middle + quick_sort(right)

print(quick_sort([24, 15, 38, 2, 19, 41, 8]))
```
Результат: `[2, 8, 15, 19, 24, 38, 41]`



🏗️ Пирамидальная сортировка (Heap Sort)
Определение:
Пирамидальная сортировка использует структуру данных «куча» (heap) для упорядочивания элементов.

Описание:
Массив превращается в max-кучу, затем извлекаются максимальные элементы по одному.

Пошаговая работа:
1. Построить max-heap из массива.
2. Поместить наибольший элемент в конец.
3. Уменьшить размер кучи и восстановить свойство max-heap.
4. Повторить, пока куча не пуста.

Сложность: 
O(n log n)

Пример:
```python
def heapify(arr, n, i):
    largest = i
    l = 2 * i + 1
    r = 2 * i + 2

    if l < n and arr[l] > arr[largest]:
        largest = l
    if r < n and arr[r] > arr[largest]:
        largest = r
    if largest != i:
        arr[i], arr[largest] = arr[largest], arr[i]
        heapify(arr, n, largest)

def heap_sort(arr):
    n = len(arr)
    for i in range(n // 2 - 1, -1, -1):
        heapify(arr, n, i)
    for i in range(n - 1, 0, -1):
        arr[i], arr[0] = arr[0], arr[i]
        heapify(arr, i, 0)
    return arr

print(heap_sort([4, 10, 3, 5, 1]))
```
Результат: `[1, 3, 4, 5, 10]`
